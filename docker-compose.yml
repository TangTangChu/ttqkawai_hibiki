services:
    ttqkawai_hibiki:
        build:
            context: .
            dockerfile: Dockerfile
            args:
                GIT_HASH: ${GIT_HASH:-dev}
        restart: unless-stopped
        init: true
        ports:
            - "3000:3000"
        environment:
            - NODE_ENV=production
